name: "CodeQL"

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "**/*.md"
  push:
    branches: [main]
  schedule:
    - cron: "41 3 * * 1" # weekly, Monday 03:41 UTC

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (java-kotlin)
    runs-on: ubuntu-latest
    # ── TEMPORARY PARK: CodeQL's Kotlin extractor does not yet support Kotlin 2.4.0 ──
    # The traced `compileKotlinJvm` below crashes inside CodeQL's instrumented build on 2.4.0
    # ("Daemon compilation failed") while the real JVM compile is green (build-linux, detekt,
    # Android all pass). Upstream tracking: https://github.com/github/codeql/issues/21938.
    # While parked, the failing steps are non-fatal so this required check stays green; scanning
    # runs best-effort and resumes strictly the moment upstream ships support.
    #
    # This is a scoped, source-analysis-only gap — supply-chain scanning (OSV / SBOM) and all
    # build/test/lint gates remain strict and unaffected.
    #
    # RE-ENABLE (when CodeQL supports 2.4.0): set repo variable CODEQL_KOTLIN_240_SUPPORTED=true
    # to restore strict failure, confirm a green run, then delete both the variable and this block.
    permissions:
      security-events: write # upload CodeQL results to code scanning
      actions: read
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up JDK 21
        uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          distribution: "zulu"
          java-version: "21"
          cache: gradle

      - name: Initialize CodeQL
        uses: github/codeql-action/init@54f647b7e1bb85c95cddabcd46b0c578ec92bc1a # v4.36.3
        with:
          languages: java-kotlin
          # Kotlin extraction requires a traced compile — build-mode "none" sees no source
          # in an all-Kotlin project ("no source code seen"). Compile the JVM target under
          # CodeQL's tracer instead; this covers commonMain + the jvm/android actuals (the
          # security-relevant glue). Kotlin/Native + JS/WASM actuals are not analyzable by CodeQL.
          build-mode: manual
          queries: security-extended

      # buffer-crypto applies com.ditchoom.boringssl.provision unconditionally, so the traced
      # compileKotlinJvm fails at configuration time until the plugin resolves. CodeQL only compiles the
      # JVM target — it never executes the Linux native cinterop — so stage the plugin + BOM only (no
      # bundle). $BORINGSSL_ARGS carries -PboringsslPluginVersion=0.0.1-rc.1. Remove once the plugin is
      # on the Plugin Portal.
      - name: Stage boringssl-kmp prerelease (plugin only)
        uses: ./.github/actions/boringssl-prerelease

      - name: Compile Kotlin (JVM) for extraction
        # Parked for Kotlin 2.4.0 (see job banner). Non-fatal until CODEQL_KOTLIN_240_SUPPORTED=true.
        continue-on-error: ${{ vars.CODEQL_KOTLIN_240_SUPPORTED != 'true' }}
        run: ./gradlew compileKotlinJvm --no-daemon --no-configuration-cache $BORINGSSL_ARGS

      - name: Perform CodeQL analysis
        # Parked for Kotlin 2.4.0 (see job banner). Non-fatal until CODEQL_KOTLIN_240_SUPPORTED=true.
        continue-on-error: ${{ vars.CODEQL_KOTLIN_240_SUPPORTED != 'true' }}
        uses: github/codeql-action/analyze@54f647b7e1bb85c95cddabcd46b0c578ec92bc1a # v4.36.3
        with:
          category: "/language:java-kotlin"
